import { TransDataInterceptor } from './trans-data.interceptor';

describe('TransDataInterceptor', () => {
  it('should be defined', () => {
    expect(new TransDataInterceptor()).toBeDefined();
  });
});
